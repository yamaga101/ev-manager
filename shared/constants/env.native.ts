// Mobile (Metro/Hermes) — import.meta.env is not supported
// Actual values come from settings store (AsyncStorage)
export const ENV_GAS_SHARED_TOKEN: string = "";
export const ENV_GEMINI_API_KEY: string = "";
